# HBNU DED Monitoring System - 트러블슈팅 히스토리

> 📅 문서 생성일: 2024년 12월 16일  
> 📝 이 문서는 프로젝트 개발 과정에서 발생한 주요 이슈와 해결 방법을 정리한 것입니다.

---

## 📋 목차

1. [프레임워크 전환 이슈](#1-프레임워크-전환-이슈)
2. [센서 연결 및 통신 이슈](#2-센서-연결-및-통신-이슈)
3. [카메라 스트리밍 이슈](#3-카메라-스트리밍-이슈)
4. [HXAPI 32비트 호환성 이슈](#4-hxapi-32비트-호환성-이슈)
5. [UI/UX 레이아웃 이슈](#5-uiux-레이아웃-이슈)
6. [자동저장 시스템 이슈](#6-자동저장-시스템-이슈)
7. [NC코드 파싱 이슈](#7-nc코드-파싱-이슈)

---

## 1. 프레임워크 전환 이슈

### 🔴 문제 상황
- **커밋**: `8795d7e`, `f8b2926`
- 초기 PyQt/PySide2 기반 UI의 한계점 발생
  - 현대적인 UI 디자인 구현의 어려움
  - 반응형 레이아웃 처리의 복잡성
  - 유지보수 및 확장성 문제

### ✅ 해결 방법
1. **React + Electron 기반으로 완전 전환**
   - PyQt 관련 파일 전체 삭제 (약 20,000줄 이상 제거)
   - React + TypeScript + Tailwind CSS로 현대적 UI 구현
   - Electron 통합으로 데스크톱 애플리케이션 지원

2. **삭제된 파일들**
   - `UI/` 디렉토리 전체 (PyQt UI 파일들)
   - 루트의 `main.py`, `icon_rc.py`, `settings.py` 등
   - `build.bat`, `HBU_Monitoring.spec` 등 빌드 파일

3. **Tailwind CSS 버전 문제** (`a6489a8`)
   - Tailwind v4에서 v3로 다운그레이드하여 안정성 확보

### 📝 교훈
> 프레임워크 선택 시 장기적인 유지보수성과 팀의 기술 스택을 고려해야 함

---

## 2. 센서 연결 및 통신 이슈

### 🔴 문제 상황 1: 좌표값이 표시되지 않음
- **커밋**: `2d48ee7`, `8c5ac12`
- CNC에서 좌표값을 읽어오지만 화면에 표시되지 않음
- 다른 센서들(레이저 파워, 멜팅풀 온도)이 작동하지 않음

### ✅ 해결 방법
```python
# Sensors/cnc_comm.py 수정
# 데이터 파싱 로직 개선
```
- CNC 통신 프로토콜 디버깅 및 수정
- 레이저 통신 모듈의 데이터 읽기 로직 수정

---

### 🔴 문제 상황 2: 센서 연결 실패 시 서버 크래시
- **커밋**: `1330621`
- 센서가 연결되지 않은 상태에서 서버 시작 시 전체 시스템 종료

### ✅ 해결 방법
```python
# 기존 (문제 코드)
if not sensor.connect():
    sys.exit(1)  # 서버 전체 종료

# 개선된 코드
if not sensor.connect():
    raise Exception("Sensor connection failed")  # 예외 처리로 변경
```
- `sys.exit()` 대신 `Exception`을 발생시켜 graceful한 에러 처리
- 센서 없이도 테스트 가능한 백엔드 모드 추가

### 📝 교훈
> 하드웨어 의존성이 있는 시스템에서는 항상 폴백(fallback) 메커니즘을 구현해야 함

---

## 3. 카메라 스트리밍 이슈

### 🔴 문제 상황
- **커밋**: `fc2fa81`
- 카메라 영상이 끊기는 현상 발생
- WebSocket 기반 이미지 전송의 불안정성

### ✅ 해결 방법
1. **MJPEG 스트리밍 방식으로 변경**
   ```python
   # backend/main.py
   # 새로운 엔드포인트 추가
   @app.get("/api/stream/{image_type}")
   async def stream_camera(image_type: str):
       # MJPEG 스트리밍 구현
   ```

2. **프론트엔드 CameraView 컴포넌트 수정**
   ```tsx
   // 기존: WebSocket으로 이미지 수신
   // 변경: MJPEG 스트리밍 URL 직접 사용
   <img src={`/api/stream/${cameraType}`} />
   ```

### 📝 결과
- 영상 끊김 현상 해결
- 더 안정적인 실시간 스트리밍 구현

---

## 4. HXAPI 32비트 호환성 이슈

### 🔴 문제 상황
- **커밋**: `1f3c374`
- HXAPI DLL이 32비트만 지원
- 64비트 Python 환경에서 DLL 로드 실패

### ✅ 해결 방법
1. **32비트 Python 서브프로세스 매니저 구현**
   ```
   backend/cnc_subprocess_manager.py (162줄 추가)
   ```

2. **별도의 32비트 실행 스크립트 생성**
   ```batch
   # start_backend_32bit.bat
   # 32비트 Python 환경에서 CNC 통신 담당
   ```

3. **관련 문서화**
   - `HXAPI_BIT_COMPATIBILITY_ISSUES.md` (235줄)
   - `SETUP_32BIT_PYTHON.md` (279줄)
   - `HBNU_MONITORING_ARCHITECTURE.md` (1,072줄)

### 📝 아키텍처
```
┌─────────────────────────────────────────────┐
│           메인 백엔드 (64비트)               │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │    CNC Subprocess Manager           │   │
│  │    (32비트 프로세스 관리)             │   │
│  └──────────────┬──────────────────────┘   │
│                 │ IPC                       │
│  ┌──────────────▼──────────────────────┐   │
│  │    32비트 Python + HXAPI            │   │
│  │    (CNC 통신 담당)                   │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 5. UI/UX 레이아웃 이슈

### 🔴 문제 상황들
- **커밋**: `9a0cf4d`, `4adaafd`, `d518bc6`, `1d2725b`, `ca47a9e`, `ad28d01`
- 좌표 축 일부 누락 (a, c축)
- 작은 화면에서 레이아웃 깨짐
- 그래프 영역이 너무 작음
- 센서 연결 상태 표시 불일치

### ✅ 해결 과정

| 커밋 | 해결 내용 |
|------|-----------|
| `9a0cf4d` | 상대좌표/절대좌표에 a, c축 복원 (5축 완전 표시) |
| `4adaafd` | 헤더에 실시간 시간 표시, 시스템 상태 재배치 |
| `d518bc6` | Feed Rate 카드 → 센서 연결 상태로 교체 |
| `1d2725b` | 2열 3행 레이아웃으로 재구성, 반응형 대응 |
| `ca47a9e` | Figma 디자인 기반 사이드바 구현 |
| `ad28d01` | 헤더바 생성 및 비율 조정 |

### ✅ 최종 레이아웃 구조
```
┌─────────────────────────────────────────────────┐
│                    헤더바                        │
├──────────────┬──────────────────────────────────┤
│              │  Basler Camera  │  ToolPath      │
│   시스템     ├─────────────────┼────────────────┤
│   상태       │  HikRobot Cam   │  Melt Pool     │
│   (좌표,     ├─────────────────┼────────────────┤
│    센서)     │  Temp/Laser     │  Height        │
└──────────────┴──────────────────┴────────────────┘
```

---

## 6. 자동저장 시스템 이슈

### 🔴 문제 상황
- **커밋**: `1330621`, `df3c8f9`
- 수동 저장만 가능하여 데이터 손실 위험
- 저장 중에 추가 저장 불가

### ✅ 해결 방법
1. **로그 파일 모니터링 기반 자동저장 구현**
   ```typescript
   // frontend/src/hooks/useAutoSave.ts
   // 자동저장 로직 구현 (211줄)
   ```

2. **수동 저장 버튼 개선**
   - 자동저장 진행 중에도 수동 저장 가능
   - 저장 상태 시각적 피드백

3. **NC 로직 제거** (`df3c8f9`)
   - 불필요한 NC 코드 관련 로직 정리
   - 저장 버튼 로직 단순화

---

## 7. NC코드 파싱 이슈

### 🔴 문제 상황
- **커밋**: `e3b6b2b`
- NC코드 파일 형식이 다양함
- ToolPath 진행상황 시각화 필요

### ✅ 해결 방법
1. **다중 형식 지원 파서 구현**
   ```python
   # backend/gcode_parser.py (263줄)
   # G-code, NC코드 다양한 형식 파싱
   ```

2. **ToolPath 시각화 컴포넌트**
   ```tsx
   // frontend/src/components/ToolPath.tsx (293줄)
   // 거리 기반 진행률 표시
   // 실시간 현재 위치 표시
   ```

3. **파일 선택 기능 개선**
   - 폴더 선택 → 파일 직접 선택으로 변경
   - Electron IPC를 통한 파일 다이얼로그

---

## 📊 커밋 통계 요약

| 카테고리 | 커밋 수 | 주요 변경 |
|----------|---------|-----------|
| UI/UX 개선 | 10 | 레이아웃, 반응형, 디자인 |
| 센서 연결 | 4 | CNC, 레이저, 온도 센서 |
| 프레임워크 | 3 | PyQt → React 전환 |
| 기능 추가 | 3 | 자동저장, NC파싱, MJPEG |

---

## 🔧 향후 개선 사항

1. **테스트 코드 보강**
   - 센서 모의(Mock) 테스트 확대
   - E2E 테스트 추가

2. **에러 처리 강화**
   - 네트워크 끊김 대응
   - 센서 재연결 자동화

3. **성능 최적화**
   - 데이터 버퍼링 개선
   - 메모리 사용량 모니터링

---

## 📚 관련 문서

- [HBNU_MONITORING_ARCHITECTURE.md](./HBNU_MONITORING_ARCHITECTURE.md) - 시스템 아키텍처
- [HBU_MONITORING_SENSOR_CONNECTION_DETAIL.md](./HBU_MONITORING_SENSOR_CONNECTION_DETAIL.md) - 센서 연결 상세
- [HXAPI_BIT_COMPATIBILITY_ISSUES.md](./HXAPI_BIT_COMPATIBILITY_ISSUES.md) - 32비트 호환성
- [SETUP_32BIT_PYTHON.md](./SETUP_32BIT_PYTHON.md) - 32비트 Python 설정
- [DED_RECIPE_INFO.md](./Sensors/DED_RECIPE_INFO.md) - 피더/가스 레지스터 정보

---

> 💡 **팁**: 새로운 이슈가 발생하면 이 문서에 추가하여 팀원들과 공유하세요!
